# Core Async Web Framework
fastapi==0.115.0
uvicorn[standard]==0.32.0  # includes httptools, websockets, h11

# Async Database
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0

# Redis (async & sync support, with high-performance parser)
redis[hiredis]==5.2.0        # includes aioredis >=2.0 under the hood
arq~=0.26.3

# MinIO / S3-compatible object storage
minio==7.2.10

# HTTP client (optional but recommended for services)
httpx==0.27.2
python-multipart~=0.0.20

# milvus
pymilvus==2.6.3

# doc
pillow~=12.0.0
python-docx~=1.2.0
tiktoken~=0.12.0

# Data validation / utilities
pydantic==2.10.0
pydantic-settings==2.6.0     # for config management

# Logging / Monitoring (optional but production-recommended)
structlog==24.4.0
orjson~=3.10.9               # faster JSON for structlog/FastAPI

# Security (JWT, etc.)
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Testing (optional dev)
httpx==0.27.2
pytest==8.3.3
pytest-asyncio==0.24.0
